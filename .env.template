# Copy this file to .env and customize as needed.
# These values are read by both the TV host and mobile apps at build time.
# Server settings (port, room capacity, …) live in apps/server/.env — see
# apps/server/.env.template.

# Default language for all apps (en, it)
DEFAULT_LANG=en
